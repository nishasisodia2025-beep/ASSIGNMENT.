def row_wise_sum(matrix):
    # Sum up each list component inside the matrix
    return [sum(row) for row in matrix]

# --- Example Usage ---
matrix = [,
 ,
    [7, 8, 9]
]

sums = row_wise_sum(matrix)
for i, r_sum in enumerate(sums):
    print(f"Sum of Row {i+1}: {r_sum}")