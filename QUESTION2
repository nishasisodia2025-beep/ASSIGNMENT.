def reverse_string(string):
    reversed_text = ""
    # Loop backward from index length-1 down to 0
    for i in range(len(string) - 1, -1, -1):
        reversed_text += string[i]
    return reversed_text

# --- Example Usage ---
text = "Adobe"
print(f"Reversed '{text}':", reverse_string(text))