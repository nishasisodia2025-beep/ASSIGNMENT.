def find_length(string):
    count = 0
    # Loop through every character to increment the counter
    for char in string:
        count += 1
    return count

# --- Example Usage ---
text = "Microsoft"
print(f"Length of '{text}':", find_length(text))