def print_repeated_number_triangle(rows):
    for i in range(1, rows + 1):
        # Convert the row number to a string and repeat it i times
        print(str(i) * i)

# Test the program
num_rows = int(input("Enter number of rows: "))
print_repeated_number_triangle(num_rows)