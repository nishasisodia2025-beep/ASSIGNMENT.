def remove_spaces(string):
    # Join characters together only if they are not spaces
    return "".join(char for char in string if char != " ")

# --- Example Usage ---
text = "  Adobe   Paytm  "
print("Without spaces:", remove_spaces(text))